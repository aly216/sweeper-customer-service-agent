# 演示问题清单（Agent 标志性测试问题）

> 覆盖智能体的 4 类场景：天气查询、报告生成、故障排查、知识问答。
> 每条标注「预期行为」和「涉及工具」，已截图的附上运行截图。

---

## 1. 天气查询（多轮 + 城市强约束）

**你：** 今天适合扫地吗？

**预期行为：** Agent 先反问「请问您所在的城市是哪里呢？」，不编造城市。

**你再回：** 北京

**预期行为：** 调用 `get_weather`，报出北京实时温度/湿度 + 未来几小时预报，并给出「适不适合扫地」的建议。

**涉及工具：** `get_weather`、`rag_summarize`

![天气查询](img/q1-weather.png)

---

## 2. 生成报告（动态提示词切换 + 报告流程）

**你：** 给我出一份这个月的使用报告

**预期行为：** 自动走 `get_user_id → get_current_month → fill_context_for_report → fetch_external_data`，最后输出 Markdown 格式的《扫地机器人使用情况报告与保养建议》。

**涉及工具：** `get_user_id`、`get_current_month`、`fill_context_for_report`、`fetch_external_data`、`rag_summarize`

![报告生成-工具调用](img/q2-report-1.png)

![报告生成-报告正文](img/q2-report-2.png)

---

## 3. 故障排查（RAG 检索）

**你：** 扫地机器人滚刷被头发缠住报警了，怎么办？

**预期行为：** 调用 `rag_summarize` 检索故障处理知识，给出具体解决步骤。

**涉及工具：** `rag_summarize`

![故障排查](img/q3-troubleshoot.png)

---

## 4. 选购建议（RAG 检索，未截图）

**你：** 家里养猫，毛特别多，选哪款扫地机器人？

**预期行为：** 调用 `rag_summarize` 检索选购指南，结合宠物毛发场景给建议。

**涉及工具：** `rag_summarize`

---

## 5. 维护保养（RAG 检索，未截图）

**你：** 滤网和边刷多久换一次？

**预期行为：** 调用 `rag_summarize` 检索维护保养知识。

**涉及工具：** `rag_summarize`

---

## 6. 边界兜底

**你：** 今天几号？我能退货吗？

**预期行为：** 诚实说明超出能力范围，而不是瞎编。

**涉及工具：** `rag_summarize`

![边界兜底](img/q6-fallback.png)
